export const selectPatientHistory = state => state.patientHistory;

/* export function selectPatientHistory(state) {
  // console.log("STATE", state.patientHistory);
  return state.patientHistory;
} */